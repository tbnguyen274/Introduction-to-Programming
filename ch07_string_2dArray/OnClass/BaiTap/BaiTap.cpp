#include "MyFunctions.h"

int main() {
	char s[100];
	int countChar = 100;
	stringInput(s, countChar);
	//convertLowerCase(s);
	// convertUpperCase(s);
	// upperFirstWord(s);
	removeMultiSpace(s);
	return 0;
}